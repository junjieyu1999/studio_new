// Shared surface colours for the 3D gallery. Kept in a leaf module so the wall
// components and the scene can share them without a circular import.

/** Off-white gallery wall — constant across day / golden hour / night. */
export const WALL_COLOR = "#FAF9F6";

/** Glass-roof mullions. */
export const BEAM_COLOR = "#cfc5b1";
